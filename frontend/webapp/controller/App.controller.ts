import BaseController from "./BaseController";

/**
 * @namespace ui.calendar.controller
 */
export default class App extends BaseController {
  public onInit(): void {
    // Root container. Pages are mounted by the router via manifest.json.
  }
}
